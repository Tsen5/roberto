import styled from '@emotion/styled';

const AskInput = styled.input(({ theme }) => ({
  all: 'unset',
  alignSelf: 'stretch',
  flexGrow: 1,
  color: theme.colors.palette.darkGrey,
}));

export default AskInput;
